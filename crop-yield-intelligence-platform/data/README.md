# Data Directory

Raw and processed datasets are deliberately excluded from Git.

Planned local structure:

```text
data/
├── raw/          # Original, unchanged source files
├── interim/      # Intermediate transformation outputs
└── processed/    # Model-ready datasets
```

The repository will contain instructions and validation reports, not
unlicensed copies of the Kaggle dataset.
